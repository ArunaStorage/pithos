# pithos_pyo3 status

`pithos_pyo3` is an unpublished empty Rust library stub in this workspace. It has no PyO3 dependency, Python extension module, Python package, or supported Python API.

Use [pithos_lib on docs.rs](https://docs.rs/pithos_lib/) for the current Rust API. This README intentionally makes no installation, binding, or release claim for `pithos_pyo3`.
